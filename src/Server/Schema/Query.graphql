type Query {
  listAllSeller: [Seller!]!,
  listAllCustomer: [Customer!]!,
  listAllMatch: [Match!]!,
  listAllSigned: [Match!]!,
  listTopSellerMatch(sellerId: ID!, num: Int): [Match!]!,
  listTopCustomerMatch(CustomerId: ID!, num: Int): [Match!]!,
  me: UserInfo!
}
